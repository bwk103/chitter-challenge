<li class="user">
  <div class="card">
    <div class="card-block">
      <div class="card-body">
        <h5 class="card-title"><%= user.name %><span class="text-muted"> @<%= user.username %></span>
          <img id="profile_image" src="<%= user.profile_image_url %>">
        </h5>
      </div>
    </div>
  </div>
</li>
